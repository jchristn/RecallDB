<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>disable</ImplicitUsings>
    <Nullable>disable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Touchstone.Core" Version="0.1.12" />
    <PackageReference Include="Voltaic" Version="2.0.0" />
  </ItemGroup>

  <ItemGroup>
    <!-- Used only by the opt-in schema cases in HybridSearchSuites (RECALLDB_TEST_DB), which call the
         driver's startup schema pass directly and inspect the database with Npgsql. -->
    <ProjectReference Include="..\RecallDb.Core\RecallDb.Core.csproj" />
  </ItemGroup>

</Project>
